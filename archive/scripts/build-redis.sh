#!/bin/bash

CONTAINER_NAME=bt-redis
CONTAINER_PORT=6379
HOST_PORT=6379
CONTAINER_PASSWORD=redsession1
IMAGE_NAME=redis


docker container run -d \
    --name "$CONTAINER_NAME" \
    -p "$HOST_PORT":"$CONTAINER_PORT" \
    -e REDIS_PASSWORD="$CONTAINER_PASSWORD" \
    "$IMAGE_NAME"
