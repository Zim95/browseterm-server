Generic single-database configuration.

# COMMANDS
----------
1. Create new migrations directory:
    - alembic init migrations
2. Create revisions:
    - alembic revision --autogenerate -m "<message>"
3. Apply the migrations:
    - alembic upgrade heads